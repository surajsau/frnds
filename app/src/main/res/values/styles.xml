<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="AppDialogTheme" parent="Theme.AppCompat.Dialog">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="AppImageButtonStyle">
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/textPrimary</item>
        <item name="android:textSize">12sp</item>
        <item name="android:background">@null</item>
    </style>

    <style name="styleChatMessage">
        <item name="android:textSize">13sp</item>
        <item name="android:layout_marginLeft">10dp</item>
    </style>

    <style name="styleFrndChatMessage" parent="styleChatMessage">
        <item name="android:textColor">@color/textFrnd</item>
    </style>

    <style name="styleMeChatMessage" parent="styleChatMessage">
        <item name="android:textColor">@color/textMe</item>
    </style>

</resources>
